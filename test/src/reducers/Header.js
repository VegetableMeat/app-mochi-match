const initialState = {
    title: 'もちまっちげーみんぐ',
    test: 'test'
}

const testReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default testReducer;