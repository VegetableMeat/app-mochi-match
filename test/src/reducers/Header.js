const initialState = {
    title: 'もちまっちげーみんぐ',
    test: 'test'
}

const header = (state = initialState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}

export default header;